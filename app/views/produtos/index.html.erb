<p id="notice"><%= notice %></p>

<h1> Product </h1>

<table>
  <thead>
    <tr>
      
    </tr>
  </thead>

  <tbody>
    <% @produtos.each do |produto| %>
      <tr>
        <td><%= produto.descricao %></td>
        <td><%= produto.quantidade %></td>
        <td><%= produto.categoria %></td>
        <td><%= produto.subcategoria %></td>
        <td><%= produto.valor %></td>
        <td><%= produto.tamanho %></td>
        <td><%= link_to 'Show', produto %></td>
        <td><%= link_to 'Edit', edit_produto_path(produto), id: "e-#{produto.descricao}" %></td>
        <td><%= link_to 'Destroy', produto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_produto_path %>
<%= link_to 'Start', root_path %>
